# Copy to .env and fill in your values.
# .env should be in .gitignore — never commit your secrets.

ANTHROPIC_API_KEY=sk-ant-...
TELEGRAM_BOT_TOKEN=...

# Path to your Obsidian vault on the host machine.
# This gets mounted into the container at /vault.
VAULT_PATH=/Users/yourname/Documents/my-vault
